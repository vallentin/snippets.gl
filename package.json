{
    "scripts": {
        "build": "gulp build",
        "rebuild": "gulp rebuild",
        "serve": "nodemon --watch gulpfile.js --exec \"gulp serve\"",
        "serve-no-open": "nodemon --watch gulpfile.js --exec \"gulp serve --no-open\"",
        "clean": "gulp clean"
    },
    "devDependencies": {
        "browser-sync": "^3.0.4",
        "del": "^8.0.0",
        "gulp": "^5.0.1",
        "gulp-htmlmin": "^5.0.1",
        "gulp-sass": "^6.0.1",
        "gulp-terser": "^2.1.0",
        "nodemon": "^3.1.10",
        "sass": "^1.89.2",
        "terser": "^5.43.1"
    }
}